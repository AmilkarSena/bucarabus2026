# Variables de entorno — App Pasajero
# Copia este archivo como .env y ajusta los valores

# URL de la API backend (sin /api al final)
VITE_API_URL=http://localhost:3001

# URL del servidor WebSocket (Socket.io) para buses en tiempo real
VITE_WS_URL=http://localhost:3001
